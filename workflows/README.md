# Workflows

Runtime workflow files and generated workflow assets can be placed here.

The Docker images copy this directory into `/app/workflows`, and the local
Compose stack bind-mounts it for development.
